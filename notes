
With react-redux we dont need to pass the callback function (or the handler) from the 
very top (means form the parent component) to the very bottom ( means to the child component) instead the child
components can call the dispatch 


